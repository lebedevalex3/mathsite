DATABASE_URL="postgresql://mathsite:mathsite@localhost:5432/mathsite?schema=public"
# Dev-only helper for POST /api/teacher/become (leave disabled in production)
# ALLOW_DEV_BECOME_TEACHER=1
# Dev-only helper for POST /api/admin/become (leave disabled in production)
# ALLOW_DEV_BECOME_ADMIN=1
