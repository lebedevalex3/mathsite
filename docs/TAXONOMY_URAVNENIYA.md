# Таксономия (MVP): Уравнения, 5 класс

## Topic

- `topic_id`: `math.equations`
- Название: `Уравнения`
- Класс: `5`

## Skill IDs (операции)

- `math.equations.understand_equation_and_root` — понимать, что такое уравнение и корень уравнения
- `math.equations.check_root` — проверять, является ли число корнем уравнения
- `math.equations.solve_x_plus_minus_a_equals_b` — решать уравнения вида x + a = b и x - a = b
- `math.equations.solve_a_plus_minus_x_equals_b` — решать уравнения вида a + x = b и a - x = b
- `math.equations.solve_multiplication_and_division_equations` — решать уравнения вида a·x = b и x : a = b
- `math.equations.solve_basic_word_equations` — составлять и решать простые уравнения по условию

## Правила стабильности ID

- `topic_id` фиксирован для этой темы: `math.equations`
- `skill_id` формат: `math.equations.<operation_name>`
- `task_id` формат: `math.equations.<skill>.000001` (ровно 6 цифр в конце)
- После публикации `task_id` не переиспользуется и не меняется
