import { KeyIdea, Steps, Mistakes } from "@/src/components/mdx";
import { SubtopicPageTemplate } from "../subtopic-page-template";

export default async function InverseSubtopicPage({ params }) {
  const { locale } = await params;

  return (
    <SubtopicPageTemplate
      locale={locale}
      slug="inverse"
      intro="Подтема про обратную пропорциональность и типовые задачи, где увеличение одной величины уменьшает другую."
    >
      <h2 id="toc">Содержание</h2>
      <ul>
        <li><a href="#ideya">Идея</a></li>
        <li><a href="#algoritm">Алгоритм</a></li>
        <li><a href="#oshibki">Типичные ошибки</a></li>
      </ul>

      <h2 id="ideya">Идея</h2>
      <KeyIdea title="Обратная пропорциональность">
        <p>
          Если одна величина увеличивается, а другая при этом уменьшается так, что
          их произведение остаётся постоянным, это обратная пропорциональность.
        </p>
      </KeyIdea>

      <h2 id="algoritm">Алгоритм</h2>
      <Steps>
        <li>Проверь, что величины меняются в противоположных направлениях.</li>
        <li>Составь пропорцию или используй постоянство произведения.</li>
        <li>Найди неизвестное и выполни проверку.</li>
      </Steps>

      <h2 id="oshibki">Типичные ошибки</h2>
      <Mistakes>
        <li>Применено правило прямой пропорциональности вместо обратной.</li>
        <li>Нет проверки результата по смыслу задачи.</li>
      </Mistakes>
    </SubtopicPageTemplate>
  );
}

