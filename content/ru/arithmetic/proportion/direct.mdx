import { KeyIdea, Steps, Mistakes } from "@/src/components/mdx";
import { SubtopicPageTemplate } from "@/src/app/[locale]/5-klass/proportion/subtopic-page-template";

export const subtopicMeta = {
  title: "Прямая пропорциональность",
  slug: "direct",
  order: 20,
};

export default async function DirectSubtopicPage({ params }) {
  const { locale } = await params;

return (

<SubtopicPageTemplate
      locale={locale}
      slug="direct"
      intro="Подтема про прямую пропорциональность: как величины меняются вместе и как это использовать в записях отношений."
    >
<h2 id="toc">Содержание</h2>
<ul>
<li><a href="#opredelenie">Определение / идея</a></li>
<li><a href="#algoritm">Алгоритм</a></li>
<li><a href="#tipichnye-oshibki">Типичные ошибки</a></li>
</ul>

      <h2 id="opredelenie">Определение / идея</h2>
      <KeyIdea title="Прямая пропорциональность">
        <p>
          Если одна величина увеличивается в несколько раз и другая увеличивается
          во столько же раз, величины связаны прямой пропорциональностью.
        </p>
      </KeyIdea>

      <h2 id="algoritm">Алгоритм</h2>
      <Steps>
        <li>Определи, что величины меняются в одном направлении.</li>
        <li>Составь пропорцию с одинаковым порядком величин.</li>
        <li>Найди неизвестное и проверь ответ по смыслу.</li>
      </Steps>

      <h2 id="tipichnye-oshibki">Типичные ошибки</h2>
      <Mistakes>
        <li>Перепутана прямая и обратная пропорциональность.</li>
        <li>Отношения записаны в разном порядке.</li>
      </Mistakes>
    </SubtopicPageTemplate>

);
}
